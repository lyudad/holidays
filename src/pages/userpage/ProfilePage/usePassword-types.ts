export type FormValues = {
  firstName: string;
  lastName: string;
  email: string;
};
export type UserValues = {
  firstName: string;
  lastName: string;
  email: string;
};
export type SendValues = {
  first_name: string;
  last_name: string;
  email: string;
};
