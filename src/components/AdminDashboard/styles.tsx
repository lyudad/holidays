import styled from 'styled-components';
import {
  fontPrimary,
  textPrimary,
  colorPrimaryText,
} from 'utils/css-constants';

export const Table = styled.main`
  width: 100%;
  height: 100%;
  max-width: 1200px;
`;
