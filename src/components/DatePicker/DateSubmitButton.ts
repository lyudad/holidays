import { Button } from 'antd';
import s from "./DatePicker.module.css";

const CalendarButton = () => {
    return(
  <>
    <Button className="DateButton" type="primary">Submit Button</Button>
  mountNode,
);

export default CalendarButton;